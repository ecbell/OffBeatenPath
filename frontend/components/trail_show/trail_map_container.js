import { connect } from 'react-redux';
import TrailMap from './trail_map';

// const mapStateToProps = (state, ownProps) => {
//   return {
//   };
// };

// const mapDispatchToProps = dispatch => ({
// });

export default connect(null, null)(TrailMap);